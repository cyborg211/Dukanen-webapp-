import ProductCard from '@/components/ProductCard';
import {categories,products} from '@/lib/data';

export default function Marketplace(){return <div className="container"><div className="market-header"><div className="eyebrow">Browse Dukanen</div><h1 style={{fontSize:'48px'}}>Marketplace</h1><p>Search products and opportunities by category, location and condition.</p><form className="searchbox"><input name="q" placeholder="Search listings, sellers or locations"/><button>Search</button></form><div className="filterbar"><span>📍 Juba</span><span>Newest</span><span>Price</span><span>Condition</span><span>Seller type</span></div></div><div className="section-head"><div><h2>All listings</h2><p>{products.length} demo listings</p></div></div><div className="product-grid">{products.map(p=><ProductCard key={p.slug} product={p}/>)}</div><section><div className="section-head"><div><h2>Browse categories</h2></div></div><div className="category-grid">{categories.map(c=><div className="category" key={c}>{c}</div>)}</div></section></div>}
